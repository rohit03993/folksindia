<?php

return [

    /*
    | Display serial = database serial + offset (e.g. certificate books start at 150).
    */
    'serial_display_offset' => (int) env('MARKSHEET_SERIAL_DISPLAY_OFFSET', 1),

];
